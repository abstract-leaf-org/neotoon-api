spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5442/neotoon_db
    username: develop
    password: password
    driver-class-name: org.postgresql.Driver
server:
  port: 8080